<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sklad.er71">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ЭлектронныйРецепт71">
        <activity android:name=".presentation.recipe.RecipeActivity"></activity>
        <activity android:name=".presentation.recipes.PreferentialPrescriptionActivity" />
        <activity android:name=".presentation.recipes.RecipesActivity" />
        <activity android:name=".presentation.reg.RegistrationActivity" />
        <activity android:name=".presentation.login.LoginActivity" />
        <activity android:name=".presentation.menu.MenuActivity" />
        <activity android:name=".presentation.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.MainActivity" />
    </application>

</manifest>